idf.py fullclean

if [ -f build ]; then
	rm -rf build
fi

if [ -f sdkconfig ]; then
	rm sdkconfig
fi

if [ -f sdkconfig.old ]; then
	rm sdkconfig.old
fi

idf.py set-target $ESPTARGET
idf.py menuconfig
idf.py build
